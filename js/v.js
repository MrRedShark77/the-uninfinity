var app;

function loadVue() {
	app = new Vue({
	    el: "#app",
	    data: {
			player,
			FORMULA,
			TABS,
			TABS_UNLS,
			ACHIEVEMENTS,
			UPGRADE,
			buyUPG,
			notate,
			buyGenerator,
			tierGenerator,
			achImage,
        }
	})
}